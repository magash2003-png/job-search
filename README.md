# Job Search Backend

Учебный backend-проект сервиса поиска работы, написанный на Go.

## Возможности

- Регистрация пользователей
- Авторизация пользователей (JWT)
- Роли пользователей:
  - admin
  - employer
  - applicant
- Создание вакансий
- Получение списка вакансий
- Получение вакансии по ID
- Создание резюме
- Создание откликов
- Добавление вакансий в избранное
- Изменение роли пользователя (только администратором)
- PostgreSQL

## Структура проекта

```text
cmd/
internal/
    auth/
    database/
    dto/
    handler/
    middleware/
    models/
    repository/
    routes/
    service/
```

## Используемые технологии

- Go
- PostgreSQL
- pgx
- JWT

## Запуск проекта

### 1. Создайте базу данных PostgreSQL

Создайте базу данных:

```text
job_search
```

### 2. Настройте файл `.env`

Пример:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=job_search

JWT_SECRET=mysecretkey

SERVER_PORT=8080
```

### 3. Запустите проект

```bash
go run ./cmd
```

После запуска API будет доступно по адресу:

```text
http://localhost:8080
```

## Основные маршруты API

| Метод | Маршрут | Описание |
| ------ | --------------------- | ---------------------------------------------- |
| POST | `/register` | Регистрация пользователя |
| POST | `/login` | Авторизация пользователя |
| GET | `/vacancies` | Получить список вакансий |
| GET | `/vacancy?id={id}` | Получить вакансию по ID |
| POST | `/vacancies` | Создать вакансию |
| GET | `/resumes` | Получить список резюме |
| POST | `/resumes` | Создать резюме |
| GET | `/responses` | Получить список откликов |
| POST | `/responses` | Создать отклик |
| GET | `/favorites` | Получить список избранного |
| POST | `/favorites` | Добавить вакансию в избранное |
| DELETE | `/favorites?id={id}` | Удалить вакансию из избранного |
| PUT | `/users/role` | Изменить роль пользователя (только admin) |

## Авторизация

Для защищённых маршрутов необходимо передавать JWT-токен в заголовке:

```text
Authorization: Bearer <your_jwt_token>
```

## Роли пользователей

### admin

- Изменение ролей пользователей.

### employer

- Создание вакансий.

### applicant

- Создание резюме.
- Создание откликов.
- Добавление вакансий в избранное.
